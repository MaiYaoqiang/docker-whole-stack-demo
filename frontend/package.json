{
  "name": "vite-docker",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "docker-build": "npm run build && docker build -t maiyaoqiang/whole-stack-frontend .",
    "docker-run": "docker run -p 10002:80 -d --name whole-stack-frontend maiyaoqiang/whole-stack-frontend"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.0",
    "vite": "^2.7.2"
  }
}
